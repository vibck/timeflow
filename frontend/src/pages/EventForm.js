import React from 'react';
import { Typography, Box } from '@mui/material';

const EventForm = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>Termin erstellen/bearbeiten</Typography>
      <Typography variant="body1">Hier wird das Formular für Termine angezeigt.</Typography>
    </Box>
  );
};

export default EventForm;
